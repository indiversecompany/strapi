import { shallow } from 'enzyme';
import { useGlobalContext, GlobalContextProvider } from 'strapi-helper-plugin';

//import SettingsPage from '../index';

describe('<SettingsPage', () => {
  test('it shoul not crash', () => {
    // const contextValues = { formatMessage: jest.fn() };

    // jest
    //   .spyOn(GlobalContextProvider, 'useGlobalContext')
    //   .mockImplementation(() => contextValues);

    //const wrapper = shallow(<SettingsPage />);

    expect(true).toEqual(true);
  });
});
